<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head') %> 
<body>

    <%- include('./partials/header') %> 

    <main class="main-products">
        
        <section class="ultimos-lanzamientos">
            <h2>Lo más nuevo</h2>
            <% for (let i = 0; i < newModels.length; i++) {%>
                <article>
                    <a href="/products/detail/<%= newModels[i].id %>"><img src="/images/<%= newModels[i].image %>" alt="Lo más nuevo"></a>
                    <p><%= newModels[i].name%></p>
                </article>
            <% } %> 
        </section>

        <section class="favoritos">
            <h2>Lo más vendido</h2>
            <% for (let i = 0; i < favoriteProducts.length; i++) {%>
                <article>
                    <a href="/products/detail/<%= favoriteProducts[i].id %>"><img src="/images/<%= favoriteProducts[i].image %>" alt="Lo más nuevo"></a>
                    <p><%= favoriteProducts[i].name%></p>
                </article>
            <% } %> 
        </section>

    </main>

    <%- include('./partials/footer') %>

</body>
</html>